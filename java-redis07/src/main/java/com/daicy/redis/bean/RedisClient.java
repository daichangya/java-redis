//package com.daicy.redis.bean;
//
///**
// * @author: create by daichangya
// * @version: v1.0
// * @description: com.daicy.redis.bean
// * @date:11/6/20
// */
//public class RedisClient {
////    int fd;
////    dict *dict;
////    int dictid;
////    sds querybuf;
////    robj *argv[REDIS_MAX_ARGS];
////    int argc;
////    int bulklen;    /* bulk read len. -1 if not in bulk read mode */
////    list *reply;
////    int sentlen;
////    time_t lastinteraction; /* time of the last interaction, used for timeout */
////    int flags; /* REDIS_CLOSE | REDIS_SLAVE */
////    int slaveseldb; /* slave selected db, if this client is a slave */
//    private int fd;
//    private Dict dict;
//    private int dictid;
//    sds querybuf;
//    robj *argv[REDIS_MAX_ARGS];
//    private int argc;
//    private int bulklen;    /* bulk read len. -1 if not in bulk read mode */
//    list *reply;
//    private int sentlen;
//    time_t lastinteraction; /* time of the last interaction, used for timeout */
//    private int flags; /* REDIS_CLOSE | REDIS_SLAVE */
//    private int slaveseldb; /* slave selected db, if this client is a slave */
//}
